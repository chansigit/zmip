name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          args: check zmip tests
      - uses: astral-sh/ruff-action@v3
        with:
          args: format --check zmip tests

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . pytest pytest-cov
      - name: Test
        run: python -m pytest --cov=zmip --cov-report=term-missing
      - name: Build and import the wheel
        run: |
          python -m pip install build
          python -m build --wheel --outdir /tmp/wheel
          python -m pip install --force-reinstall --no-deps /tmp/wheel/*.whl
          cd /tmp && python -c "import zmip, zmip.annotate, zmip.lineage, zmip.report; from zmip.runtime import check_runtime; check_runtime()"
